// Punto 1
function findMax(entrada){
    let valorMax = entrada[0];
    for (let i = 0; i < entrada.length; i++) {
        if(valorMax < entrada[i]){
            valorMax = entrada[i];
        }
    }
    return valorMax
}

console.log(findMax([3, 17, -1, 4, -19]));

//Punto 2
function includes(lista, numero){

    for (let i = 0; i < lista.length; i++) {
        if(numero == lista[i]){
            
        }
        
    }
}
